import React from 'react';
import ProjectTable from '../Components/ProjectTable'; // Rename the import to avoid conflict

const ProjectTablePage = () => { // Rename the local function
  return (
    <>
      <ProjectTable />
    </>
  );
};

export default ProjectTablePage;
